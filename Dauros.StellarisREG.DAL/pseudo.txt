Ascetic, Hivemind, Gestalt


GetDisabledProps var currentState;
foreach(allprops)
	var shadowstate
	if (origin or authority) replace prop
	else add prop
	var valid = validate newstate
	if not valid, disable prop


		
		
		
		